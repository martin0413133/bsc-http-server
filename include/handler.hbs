#ifndef CC_HANDLER_HBS
#define CC_HANDLER_HBS
#include "../include/config.hbs"
#include "../include/router.hbs"
#include "../include/http_response.hbs"

// Functional core: pure request->response transformation (business, no _Unsafe, socket-free).
// Malformed -> 400; matched route -> handler; otherwise -> static file (200/404).
_Safe Response handle_request(const Config* _Borrow cfg, const Router* _Borrow router,
                              const char* _Nonnull raw);
#endif
