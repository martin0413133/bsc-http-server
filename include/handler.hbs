#ifndef CC_HANDLER_HBS
#define CC_HANDLER_HBS
#include "../include/http_response.hbs"
#include "../include/config.hbs"
#include "../include/router.hbs"
// 纯请求处理：解析 raw、路由、生成 Response。无 socket。调用方负责 response_free。
_Safe struct Response handle_request(const struct Config* _Borrow cfg, const struct Router* _Borrow router,
                                     const char* _Nonnull raw);
#endif
