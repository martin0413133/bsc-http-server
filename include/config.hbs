#ifndef CC_CONFIG_HBS
#define CC_CONFIG_HBS
#include "string.hbs"

_Owned struct Config {
_Public:
    int port;
    int threads;
    String document_root;
};

_Safe Config Config::default_config(void);
_Safe Config Config::parse(const char* _Nonnull text);   // parse from buffer (business; no file IO)
#endif
