#ifndef CC_CONFIG_HBS
#define CC_CONFIG_HBS
#include "string.hbs"

_Owned struct Config {
_Public:
    int port;
    int threads;
    String document_root;
    int ssl_port;
    String ssl_cert;
    String ssl_key;
};

_Safe Config Config::default_config(void);
_Safe Config Config::parse(const String* _Borrow text);   // parse from a String (business; no file IO)
#endif
