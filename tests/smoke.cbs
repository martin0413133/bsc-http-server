#include <stdio.h>
#include "string.hbs"
#include "vec.hbs"

int main(void) {
    String s = String::new();
    s.push('h'); s.push('i');
    Vec<int> v = Vec<int>::new();
    v.push(7);
    _Unsafe { printf("smoke len=%zu vec0=%d\n", s.length(), *v.get(0)); }
    return 0;
}
